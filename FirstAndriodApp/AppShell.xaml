<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FirstAndriodApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:FirstAndriodApp.Pages"
    Shell.FlyoutBehavior="Flyout"
    Shell.FlyoutBackgroundColor="#F0F4FF"
    Shell.BackgroundColor="{StaticResource GamePrimary}"
    Shell.ForegroundColor="White"
    Shell.TitleColor="White"
    Title="Kids Fun Games">

    <Shell.FlyoutHeader>
        <Grid Background="{StaticResource GamePageGradient}" Padding="20,30,20,20" HeightRequest="120">
            <VerticalStackLayout VerticalOptions="Center" Spacing="4">
                <Label Text="🎮 Kids Fun Games" FontSize="24" FontAttributes="Bold" TextColor="{StaticResource GamePrimary}" />
                <Label Text="Learn, Play and Have Fun!" FontSize="13" TextColor="{StaticResource GameText}" Opacity="0.7" />
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <ShellContent
        Title="Games"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:GamesPage}"
        Route="games" />

    <!-- 
    <ShellContent
        Title="Dashboard"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:MainPage}"
        Route="main" />
    -->

    <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}" SemanticProperties.Description="Light mode"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}" SemanticProperties.Description="Dark mode"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
