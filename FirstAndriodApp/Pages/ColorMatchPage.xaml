<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FirstAndriodApp.Pages.ColorMatchPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:FirstAndriodApp.Models"
    Title="🎨 Color Match">

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="16" Background="{StaticResource GamePageGradient}">
        <Label
            Grid.Row="0"
            Text="{Binding InstructionText}"
            Style="{StaticResource GameTitleLabel}" />

        <Label
            Grid.Row="1"
            Margin="0,8,0,12"
            Text="{Binding StatusText}"
            Style="{StaticResource GameStatusLabel}" />

        <Button
            Grid.Row="2"
            Text="🔄 Reset"
            Style="{StaticResource GameResetButton}"
            HorizontalOptions="Center"
            Clicked="Reset_Clicked" />

        <CollectionView
            Grid.Row="3"
            ItemsSource="{Binding Items}"
            SelectionMode="None"
            Margin="0,20,0,0">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="3" HorizontalItemSpacing="12" VerticalItemSpacing="12" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:MatchItem">
                    <Border
                        StrokeShape="RoundRectangle 24"
                        StrokeThickness="0"
                        BackgroundColor="White"
                        HeightRequest="100"
                        WidthRequest="100">
                        <Border.Shadow>
                            <Shadow Brush="#20000000" Offset="0,4" Opacity="0.15" Radius="10" />
                        </Border.Shadow>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer 
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.TapItemCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>
                        
                        <Grid>
                            <!-- The Item Image -->
                            <Image
                                Source="{Binding ObjectImage}"
                                Aspect="AspectFit"
                                Margin="15"
                                Opacity="{Binding IsMatched, Converter={StaticResource InvertedBoolConverter}, ConverterParameter=0.3}" /> 
                            
                            <!-- Checkmark Overlay -->
                            <Border 
                                IsVisible="{Binding IsMatched}"
                                BackgroundColor="#806BCB77" 
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 24">
                                <Label Text="✅" FontSize="36" HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
