<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FirstAndriodApp.Pages.ColorMixingPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pageModels="clr-namespace:FirstAndriodApp.PageModels"
    Title="🌈 Color Mixing">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="16" Background="{StaticResource GamePageGradient}">
        <Label
            Grid.Row="0"
            Text="🌈 Mix Colors!"
            Style="{StaticResource GameTitleLabel}" />

        <Label
            Grid.Row="1"
            Text="{Binding StatusText}"
            Style="{StaticResource GameScoreLabel}"
            HorizontalOptions="Center" 
            Margin="0,0,0,20"/>

        <VerticalStackLayout Grid.Row="2" Spacing="20" HorizontalOptions="Center" VerticalOptions="Center">
            
            <!-- Target -->
            <Label Text="🎯 Target Color" HorizontalOptions="Center" FontAttributes="Bold" FontSize="16" TextColor="{StaticResource GameText}" />
            <Border HeightRequest="85" WidthRequest="85" BackgroundColor="{Binding TargetColor}" StrokeThickness="4" Stroke="White">
                 <Border.StrokeShape><RoundRectangle CornerRadius="42"/></Border.StrokeShape>
                 <Border.Shadow>
                     <Shadow Brush="#40000000" Offset="0,4" Opacity="0.2" Radius="10" />
                 </Border.Shadow>
            </Border>

            <!-- Result -->
            <Label Text="🧪 Your Mix" HorizontalOptions="Center" Margin="0,16,0,0" FontAttributes="Bold" FontSize="16" TextColor="{StaticResource GameText}" />
            <Grid ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="12" HorizontalOptions="Center">
                <Border HeightRequest="65" WidthRequest="65" BackgroundColor="{Binding Color1}" Stroke="White" StrokeThickness="3">
                     <Border.StrokeShape><RoundRectangle CornerRadius="32"/></Border.StrokeShape>
                     <Border.Shadow>
                         <Shadow Brush="#30000000" Offset="0,3" Opacity="0.15" Radius="6" />
                     </Border.Shadow>
                </Border>
                <Label Grid.Column="1" Text="+" VerticalOptions="Center" FontSize="28" FontAttributes="Bold" TextColor="{StaticResource GamePrimary}" />
                <Border Grid.Column="2" HeightRequest="65" WidthRequest="65" BackgroundColor="{Binding Color2}" Stroke="White" StrokeThickness="3">
                     <Border.StrokeShape><RoundRectangle CornerRadius="32"/></Border.StrokeShape>
                     <Border.Shadow>
                         <Shadow Brush="#30000000" Offset="0,3" Opacity="0.15" Radius="6" />
                     </Border.Shadow>
                </Border>
            </Grid>
            
            <Label Text="=" HorizontalOptions="Center" FontSize="28" FontAttributes="Bold" TextColor="{StaticResource GamePrimary}" />
            
            <Border HeightRequest="100" WidthRequest="100" BackgroundColor="{Binding MixedColor}" StrokeThickness="4" Stroke="White">
                 <Border.StrokeShape><RoundRectangle CornerRadius="50"/></Border.StrokeShape>
                 <Border.Shadow>
                     <Shadow Brush="#40000000" Offset="0,5" Opacity="0.25" Radius="12" />
                 </Border.Shadow>
            </Border>

        </VerticalStackLayout>

        <!-- Palette -->
        <ScrollView Grid.Row="3" Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
            <HorizontalStackLayout BindableLayout.ItemsSource="{Binding AvailableColors}" Spacing="14" Padding="12" HorizontalOptions="Center">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="pageModels:ColorItem">
                        <Border HeightRequest="65" WidthRequest="65" BackgroundColor="{Binding Color}" StrokeThickness="3" Stroke="White">
                            <Border.StrokeShape><RoundRectangle CornerRadius="32"/></Border.StrokeShape>
                            <Border.Shadow>
                                <Shadow Brush="#30000000" Offset="0,3" Opacity="0.2" Radius="6" />
                            </Border.Shadow>
                             <Border.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectColorCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </HorizontalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
