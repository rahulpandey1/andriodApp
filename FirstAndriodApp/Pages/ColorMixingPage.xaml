<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FirstAndriodApp.Pages.ColorMixingPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pageModels="clr-namespace:FirstAndriodApp.PageModels"
    Title="Color Mixing">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="16" BackgroundColor="{StaticResource GameBackground}">
        <Label
            Grid.Row="0"
            Text="Mix Colors!"
            Style="{StaticResource GameTitleLabel}" />

        <Label
            Grid.Row="1"
            Text="{Binding StatusText}"
            FontSize="20"
            HorizontalOptions="Center" 
            Margin="0,0,0,20"/>

        <VerticalStackLayout Grid.Row="2" Spacing="20" HorizontalOptions="Center" VerticalOptions="Center">
            
            <!-- Target -->
            <Label Text="Target Color" HorizontalOptions="Center" />
            <Border HeightRequest="80" WidthRequest="80" BackgroundColor="{Binding TargetColor}" StrokeThickness="4" Stroke="Black">
                 <Border.StrokeShape><RoundRectangle CornerRadius="40"/></Border.StrokeShape>
            </Border>

            <!-- Result -->
            <Label Text="Your Mix" HorizontalOptions="Center" Margin="0,20,0,0"/>
            <Grid ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="10">
                <Border HeightRequest="60" WidthRequest="60" BackgroundColor="{Binding Color1}" Stroke="Gray" StrokeThickness="2">
                     <Border.StrokeShape><RoundRectangle CornerRadius="30"/></Border.StrokeShape>
                </Border>
                <Label Text="+" VerticalOptions="Center" FontSize="24" FontAttributes="Bold" />
                <Border HeightRequest="60" WidthRequest="60" BackgroundColor="{Binding Color2}" Stroke="Gray" StrokeThickness="2">
                     <Border.StrokeShape><RoundRectangle CornerRadius="30"/></Border.StrokeShape>
                </Border>
            </Grid>
            
            <Label Text="=" HorizontalOptions="Center" FontSize="24" FontAttributes="Bold" />
            
            <Border HeightRequest="100" WidthRequest="100" BackgroundColor="{Binding MixedColor}" StrokeThickness="4" Stroke="Black">
                 <Border.StrokeShape><RoundRectangle CornerRadius="50"/></Border.StrokeShape>
            </Border>

        </VerticalStackLayout>

        <!-- Palette -->
        <ScrollView Grid.Row="3" Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
            <HorizontalStackLayout BindableLayout.ItemsSource="{Binding AvailableColors}" Spacing="15" Padding="10" HorizontalOptions="Center">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="pageModels:ColorItem">
                        <Border HeightRequest="70" WidthRequest="70" BackgroundColor="{Binding Color}" StrokeThickness="2" Stroke="White">
                            <Border.StrokeShape><RoundRectangle CornerRadius="35"/></Border.StrokeShape>
                             <Border.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectColorCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </HorizontalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
