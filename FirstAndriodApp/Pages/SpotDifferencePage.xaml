<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FirstAndriodApp.Pages.SpotDifferencePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pageModels="clr-namespace:FirstAndriodApp.PageModels"
    Title="Spot Difference">

    <Grid RowDefinitions="Auto,Auto,*" Padding="16" BackgroundColor="{StaticResource GameBackground}">
        <Label
            Grid.Row="0"
            Text="Tap the odd one out!"
            Style="{StaticResource GameTitleLabel}" />

        <Label
            Grid.Row="1"
            Text="{Binding StatusText}"
            FontSize="16"
            HorizontalOptions="Center" 
            Margin="0,0,0,10"/>

        <ScrollView Grid.Row="2">
            <VerticalStackLayout Spacing="20" HorizontalOptions="Center">
                
                <!-- ORIGINAL IMAGE (Reference) -->
                <Border StrokeThickness="2" Stroke="Gray">
                    <Image Source="{Binding BaseImage}" HeightRequest="250" WidthRequest="300" Aspect="AspectFill" />
                </Border>

                <!-- MODIFIED IMAGE (Interactive) -->
                <Border StrokeThickness="4" Stroke="{StaticResource GamePrimary}">
                    <AbsoluteLayout HeightRequest="250" WidthRequest="300">
                        <!-- Background -->
                        <Image Source="{Binding BaseImage}" Aspect="AspectFill" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All" />
                        
                        <!-- Differences Overlay -->
                        <Grid AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All">
                             <BindableLayout.ItemsSource>
                                <Binding Path="Differences" />
                            </BindableLayout.ItemsSource>
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="pageModels:DifferenceItem">
                                    <Image
                                        Source="{Binding Image}"
                                        HeightRequest="40"
                                        WidthRequest="40"
                                        TranslationX="{Binding X}"
                                        TranslationY="{Binding Y}"
                                        Opacity="{Binding Opacity}"
                                        HorizontalOptions="Start"
                                        VerticalOptions="Start">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DifferenceTappedCommand}"
                                                CommandParameter="{Binding .}" />
                                        </Image.GestureRecognizers>
                                    </Image>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </Grid>
                    </AbsoluteLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
